import { Dimensions } from 'react-native';

const { height, width } = Dimensions.get('window');

export default animSettings = {
	//Change this to make animation slower/faster, default is 300ms
    animTime: 500
}